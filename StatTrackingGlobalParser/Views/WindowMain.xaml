<Window x:Class="StatTrackingGlobalParser.Views.WindowMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:StatTrackingGlobalParser.Views"
        xmlns:viewModels="clr-namespace:StatTrackingGlobalParser.ViewModels"
        xmlns:utilities="clr-namespace:StatTrackingGlobalParser.Utilities"
        mc:Ignorable="d"
        Title="{Binding Title, FallbackValue='Commandos.StatTrackingGlobalParser'}" Height="768" Width="1024">

    <Window.DataContext>
        <viewModels:VmWindowMain />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Settings:">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="2">
                    <TextBlock Text="File path:" />
                    <TextBox Text="{Binding FilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <Button Grid.Column="1" Margin="2" Content="Parse" Command="{Binding ParseCommand, Mode=OneTime}"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Results:">
            <TabControl>
                <TabItem>
                    <TabItem.Header>
                        <TextBlock>
                            <Run Text="Boolean properties" />
                            <Run Text=" (" />
                            <Run Text="{Binding BoolProperties.Count, Mode=OneWay, FallbackValue='0'}" />
                            <Run Text=")" />
                        </TextBlock>
                    </TabItem.Header>
                    <ListBox ItemsSource="{Binding BoolProperties}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Value}" Value="False">
                                                    <Setter Property="Background" Value="LightGray" />
                                                    <Setter Property="Foreground" Value="Black" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Value}" Value="True">
                                                    <Setter Property="Background" Value="LightGreen" />
                                                    <Setter Property="Foreground" Value="Black" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </TabItem>
            </TabControl>
        </GroupBox>

    </Grid>
</Window>
